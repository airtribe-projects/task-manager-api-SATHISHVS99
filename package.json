{
  "name": "to-do-api",
  "version": "1.0.0",
  "description": "Create an basic API for to-do-list with CRUD operations",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "SATHISH VS",
  "license": "ISC",
  "dependencies": {
    "express": "^4.21.0",
    "express-validator": "^7.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.7"
  }
}
